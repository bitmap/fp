import { resolve } from "./utils/resolve";

/**
 * Filters items from a list according to predicate. `filter` args are curried.
 *
 * `filter :: (a -> boolean) -> [a] -> [b]`
 */
export const filter = resolve(true);
